/**
 * Canonical production origin — used for metadata, Open Graph, canonical URLs,
 * the sitemap, and robots. Keep this as the primary domain Google should index.
 */
export const SITE_URL = "https://vaultshaker.com";
